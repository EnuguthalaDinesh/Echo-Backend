# -------------------------------------------------------------------
# CORE WEB FRAMEWORK & SERVER (Required for FastAPI/ASGI)
# -------------------------------------------------------------------
fastapi
uvicorn[standard]
gunicorn  # Recommended for production deployments like Render

# -------------------------------------------------------------------
# DATABASE & CACHING (MongoDB and Redis)
# -------------------------------------------------------------------
motor
redis[async]  # Use redis[async] or aioredis (aioredis is now just redis)

# -------------------------------------------------------------------
# AI & ML (Gemini and Hugging Face Transformers)
# -------------------------------------------------------------------
google-genai
transformers
torch
sentencepiece  # Required by some models used by transformers (e.g., BART)
protobuf       # Common dependency for Google libraries

# -------------------------------------------------------------------
# AUTHENTICATION & SECURITY
# -------------------------------------------------------------------
passlib[bcrypt]
python-jose[cryptography]
authlib
python-dotenv
python-multipart  # For handling form data and file uploads

# -------------------------------------------------------------------
# UTILITIES & DATA (Required for Pydantic/Email/BSON)
# -------------------------------------------------------------------
pydantic
email-validator  # Required for EmailStr in Pydantic models
python-dotenv    # For os.getenv/load_dotenv
bson             # For ObjectId handling (often pulled by motor, but good to list)